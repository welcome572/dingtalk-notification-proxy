app:
  name: "dingtalk-notification-proxy"
  version: "1.0.0"

dingtalk:
  webhooks:
    default:
      url: "https://oapi.dingtalk.com/robot/send?access_token="
      secret: ""

security:
  rate_limit:
    enabled: true

logging:
  level: "INFO"
